environments:
  docker:
    pipelines:
      - build-on-docker-agent
pipelines:
  build-on-docker-agent:
    group: hello-cd
    materials:
      git-repo:  # material name
        git: /source
        branch: master
    stages:
      - build: # stage name
          clean_workspace: true
          jobs:
            build-artifact: # job name
              artifacts:
                - test:
                   source: build/output/reports/*tests.xml
                   destination: "tests"
              tasks:
                - exec: # type of task
                    command: ./docker/build.sh
      - analyze: # stage name
          clean_workspace: true
          jobs:
            code-analysis:
              artifacts:
                - build:
                   source: build/output/reports
                   destination: ""
                - test:
                   source: build/output/reports/*_analysis.xml
                   destination: "tests"
              tabs:
                mylib: reports/mylib_coverage/index.html
                myutils: reports/myutils_coverage/index.html
              tasks:
                - exec:
                    command: ./docker/run-analysis.sh